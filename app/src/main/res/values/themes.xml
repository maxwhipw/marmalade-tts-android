<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Compose replaces this at runtime; XML needed
         for Activity window background to prevent flash before Compose paints). -->
    <style name="Theme.MarmaladeTts" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
